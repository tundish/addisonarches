<!--(set_escape)-->
    html
<!--(end)-->
<!--(macro nav_list)-->
<nav class="pure-menu pure-menu-open pure-menu-horizontal">
<ul>
    <!--(for view in nav)-->
    <li><dl>
        <!--(for attr, val in zip(view.fields, view.obj))-->
        <dt>@! attr !@</dt>
        <dd>@! val !@</dd>
        <!--(end)-->
    </dl></li>
    <!--(end)-->
</ul>
</nav>
<!--(end)-->
<!--(if exists("unittest"))-->
@!nav_list(nav=nav)!@
<!--(end)-->
