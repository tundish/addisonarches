<!--(set_escape)-->
    html
<!--(end)-->
<!--(macro item_list)-->
<ul>
    <!--(for data, view in items.items())-->
    <li>@!default("data.name", "Eh?")!@</li>
    <!--(end)-->
</ul>
<!--(end)-->
<!--(if exists("unittest"))-->
@!item_list(items=items)!@
<!--(end)-->
