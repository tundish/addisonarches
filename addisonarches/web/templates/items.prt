<!--(set_escape)-->
    html
<!--(end)-->
<!--(macro item_list)-->
<ul>
    <!--(for view in items)-->
    <li><dl>
        <!--(for attr, val in zip(view.fields, view.obj))-->
        <dt>@! attr !@</dt>
        <dd>@! val !@</dd>
        <!--(end)-->
    </dl></li>
    <!--(end)-->
</ul>
<!--(end)-->
<!--(if exists("unittest"))-->
@!item_list(items=items)!@
<!--(end)-->
